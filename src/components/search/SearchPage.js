import React from "react";
import styled from "styled-components";

const SearchPage = () => {
  return <SearchPageWrapper></SearchPageWrapper>;
};

const SearchPageWrapper = styled.div``;

export default SearchPage;
