import React from 'react';
import styled from 'styled-components';

const Logo = () => {
    return (
        <LogoWrapper>
            Logo
        </LogoWrapper>
    );
}

const LogoWrapper = styled.div`
    font-weight:800;
    font-size:3em;
`;

export default Logo;
