import styled from 'styled-components';

const TopBackground = () => {
  return <StyledTopBackground></StyledTopBackground>;
};

export default TopBackground;

// --- Styled Components ---
const StyledTopBackground = styled.div`

`;