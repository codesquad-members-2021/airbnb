import styled from 'styled-components';

const ContentInfo = () => {
  return <StyledContentInfo></StyledContentInfo>;
};

export default ContentInfo;

// --- Styled Components ---
const StyledContentInfo = styled.div`

`;